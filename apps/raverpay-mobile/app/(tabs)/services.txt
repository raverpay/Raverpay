// app/(tabs)/services.tsx
import React from 'react';
import { View, ScrollView } from 'react-native';
import { StatusBar } from 'expo-status-bar';
import { Ionicons } from '@expo/vector-icons';
import { Text, Card } from '@/src/components/ui';

export default function ServicesScreen() {
  return (
    <View className="flex-1 bg-gray-50">
      <StatusBar style="dark" />

      {/* Header */}
      <View className="bg-white pt-16 pb-6 px-5 border-b border-gray-200">
        <Text variant="h1">Services</Text>
        <Text variant="body" color="secondary" className="mt-1">
          Choose a service to get started
        </Text>
      </View>

      {/* Content */}
      <ScrollView className="flex-1 px-5 pt-6" showsVerticalScrollIndicator={false}>
        {/* VTU Services */}
        <View className="mb-6">
          <Text variant="h3" className="mb-4">
            VTU Services
          </Text>
          <View className="gap-3">
            <ServiceCard
              icon="call"
              title="Buy Airtime"
              description="Top up your phone or someone else's"
            />
            <ServiceCard
              icon="wifi"
              title="Buy Data"
              description="Subscribe to data bundles for all networks"
            />
          </View>
        </View>

        {/* Bill Payments */}
        <View className="mb-6">
          <Text variant="h3" className="mb-4">
            Bill Payments
          </Text>
          <View className="gap-3">
            <ServiceCard
              icon="tv"
              title="Cable TV"
              description="Pay for DStv, GOtv, and Startimes"
            />
            <ServiceCard
              icon="flash"
              title="Electricity"
              description="Buy prepaid electricity units"
            />
          </View>
        </View>
      </ScrollView>
    </View>
  );
}

interface ServiceCardProps {
  icon: keyof typeof Ionicons.glyphMap;
  title: string;
  description: string;
}

const ServiceCard: React.FC<ServiceCardProps> = ({ icon, title, description }) => (
  <Card variant="elevated" pressable onPress={() => console.log('Service pressed')}>
    <View className="flex-row items-center p-4">
      <View className="w-12 h-12 rounded-full bg-purple-100 items-center justify-center mr-4">
        <Ionicons name={icon} size={24} color="#8B5CF6" />
      </View>
      <View className="flex-1">
        <Text variant="bodyMedium">{title}</Text>
        <Text variant="caption" color="secondary" className="mt-1">
          {description}
        </Text>
      </View>
      <Ionicons name="chevron-forward" size={20} color="#9CA3AF" />
    </View>
  </Card>
);
